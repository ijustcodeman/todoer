<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_sort"
        android:title="Sortieren"
        android:icon="@android:drawable/ic_menu_sort_by_size"
        app:showAsAction="ifRoom">
        <menu>
            <item
                android:id="@+id/sort_by_date_asc"
                android:title="Datum (aufsteigend)" />
            <item
                android:id="@+id/sort_by_date_desc"
                android:title="Datum (absteigend)" />
            <item
                android:id="@+id/sort_by_priority_asc"
                android:title="Priorität (niedrig zuerst)" />
            <item
                android:id="@+id/sort_by_priority_desc"
                android:title="Priorität (hoch zuerst)" />
            <item
                android:id="@+id/sort_by_status_done"
                android:title="Status (erledigt zuerst)" />
            <item
                android:id="@+id/sort_by_status_open"
                android:title="Status (offen zuerst)" />
        </menu>
    </item>

    <item
        android:id="@+id/action_settings"
        android:title="Einstellungen"
        app:showAsAction="never" />
</menu>
